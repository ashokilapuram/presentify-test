const { createProxyMiddleware } = require('http-proxy-middleware');

module.exports = function(app) {
  // This file is used to configure the development server
  // No proxy needed for our current setup
};
